package ${package.QueryVO};

#foreach($pkg in ${importQueryVOFrameworkPackages})
import ${pkg};
#end

#foreach($pkg in ${importQueryVOJavaPackages})
import ${pkg};
#end

/**
 * $!{table.comment}-VO
 *
 * @author ${author}
 * @since ${date}
#if(${commentLink})
 * @see ${package.Entity}.${entity}
 * @see ${package.Mapper}.${mapper}
 * @see ${package.ServiceImpl}.${serviceImpl}
#end
 */
#if(${lombok})
@Data
    #if(${chainModel})
@Accessors(chain = true)
    #end
    #if(${queryVOExtendsEntity})
@EqualsAndHashCode(callSuper = true)
    #end
#end
#if(${springdoc})
@Schema(description = "$!{table.comment}-VO")
#elseif(${swagger})
@ApiModel(description = "$!{table.comment}-VO")
#end
#if(${generateExport} && ${exportOnQueryVO})
@ExcelIgnoreUnannotated
#end
#if(${queryVOExtendsEntity})
public class ${queryVO} extends ${entity} {
#else
public class ${queryVO} {
#end
## ----------  BEGIN 字段循环遍历  ----------
#if(!${queryVOExtendsEntity})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else

    #if("$!field.comment" != "")
        #if(${springdoc})
    @Schema(description = "${field.comment}")
        #elseif(${swagger})
    @ApiModelProperty("${field.comment}")
        #else
    /**
     * ${field.comment}
     */
        #end
    #end
    #if(${generateExport} && ${exportOnQueryVO})## 导出
    @ExcelProperty(value = "${field.comment}")
    #end
    private ${field.propertyType} ${field.propertyName};
#end
#end
#end
## ----------  END 字段循环遍历  ----------
#if(!${lombok})
#foreach($field in ${table.fields})
#if(${field.logicDeleteField})
#else
    #if(${field.propertyType.equals("boolean")})
        #set($getprefix="is")
    #else
        #set($getprefix="get")
    #end

    public ${field.propertyType} ${getprefix}${field.capitalName}() {
        return ${field.propertyName};
    }

    #if(${chainModel})
    public ${queryVO} set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #else
    public void set${field.capitalName}(${field.propertyType} ${field.propertyName}) {
    #end
        this.${field.propertyName} = ${field.propertyName};
     #if(${chainModel})
        return this;
    #end
    }
#end
#end
#end

}
